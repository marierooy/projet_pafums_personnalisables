<form method="post" action="{{ path('app_heart_scent_delete', {'id': heart_scent.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette note de coeur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ heart_scent.id) }}">
    <button class="btn">Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}HeartScent{% endblock %}

{% block body %}
    <h1>HeartScent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heart_scent.id }}</td>
            </tr>
            <tr>
                <th>Scent</th>
                <td>{{ heart_scent.Scent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_heart_scent_index') }}">back to list</a>

    <a href="{{ path('app_heart_scent_edit', {'id': heart_scent.id}) }}">edit</a>

    {{ include('heart_scent/_delete_form.html.twig') }}
{% endblock %}

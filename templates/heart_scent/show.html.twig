{% extends 'base.html.twig' %}

{% block title %}HeartScent{% endblock %}

{% block body %}
    <h1>HeartScent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heart_scent.id }}</td>
            </tr>
            <tr>
                <th>Senteur</th>
                <td>{{ heart_scent.Scent }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{heart_scent.image}}" width="300"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_heart_scent_index') }}">Retour à la liste des senteurs</a>

    <a href="{{ path('app_heart_scent_edit', {'id': heart_scent.id}) }}">Editer</a>

    {{ include('heart_scent/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}BaseScent{% endblock %}

{% block body %}
    <h1>BaseScent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ base_scent.id }}</td>
            </tr>
            <tr>
                <th>Scent</th>
                <td>{{ base_scent.Scent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_base_scent_index') }}">back to list</a>

    <a href="{{ path('app_base_scent_edit', {'id': base_scent.id}) }}">edit</a>

    {{ include('base_scent/_delete_form.html.twig') }}
{% endblock %}

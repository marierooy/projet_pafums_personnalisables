{% extends 'base.html.twig' %}

{% block title %}ReproducedPerfume{% endblock %}

{% block body %}
    <h1>ReproducedPerfume</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reproduced_perfume.id }}</td>
            </tr>
            <tr>
                <th>SamplingPrice</th>
                <td>{{ reproduced_perfume.samplingPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reproduced_perfume_index') }}">back to list</a>

    <a href="{{ path('app_reproduced_perfume_edit', {'id': reproduced_perfume.id}) }}">edit</a>

    {{ include('reproduced_perfume/_delete_form.html.twig') }}
{% endblock %}

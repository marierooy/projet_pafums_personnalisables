<form method="post" action="{{ path('app_created_perfume_delete', {'id': created_perfume.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ created_perfume.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_created_perfume_delete', {'id': created_perfume.id}) }}" onsubmit="return confirm('Etes-vous sure de vouloir supprimer ce produit?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ created_perfume.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>

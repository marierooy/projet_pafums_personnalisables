<form method="post" action="{{ path('app_head_scent_delete', {'id': head_scent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ head_scent.id) }}">
    <button class="btn">Delete</button>
</form>
